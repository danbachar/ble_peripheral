name: ble_peripheral_bondless
description: Ble peripheral is a Flutter plugin that allows you to use your device as Bluetooth Low Energy (BLE) peripheral, while supporting bondless communication.
version: 2.5.1
homepage: https://github.com/danbachar/ble_peripheral
repository: https://github.com/danbachar/ble_peripheral
issue_tracker: https://github.com/danbachar/ble_peripheral/issues

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  pigeon: ^17.3.0

flutter:
  plugin:
    platforms:
      android:
        package: com.rohit.ble_peripheral
        pluginClass: BlePeripheralPlugin
      ios:
        pluginClass: BlePeripheralPlugin
        sharedDarwinSource: true
      macos:
        pluginClass: BlePeripheralPlugin
        sharedDarwinSource: true
      windows:
        pluginClass: BlePeripheralPluginCApi
